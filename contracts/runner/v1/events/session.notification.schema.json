{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qory.dev/contracts/runner/v1/events/session.notification.schema.json",
  "title": "dev.qory.session.notification",
  "description": "The runtime notified its user: it waits for a permission, it is idle, it needs input.",
  "type": "object",
  "additionalProperties": false,
  "required": ["session_id", "kind", "message"],
  "properties": {
    "session_id": {"description": "The runtime's own session identifier.", "type": "string", "minLength": 1},
    "kind": {"description": "The notification type, in the runtime's words: permission_prompt, idle_prompt.", "type": "string", "minLength": 1},
    "message": {"type": "string"},
    "title": {"type": "string"},
    "agent_id": {"description": "The subagent this happened in, when it did not happen in the main conversation.", "type": "string", "minLength": 1},
    "agent_type": {"description": "The subagent's type, or the agent the session was started as.", "type": "string", "minLength": 1}
  }
}
