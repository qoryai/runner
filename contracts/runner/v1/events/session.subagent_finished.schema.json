{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qory.dev/contracts/runner/v1/events/session.subagent_finished.schema.json",
  "title": "ai.qory.session.subagent_finished",
  "description": "A subagent finished.",
  "type": "object",
  "additionalProperties": false,
  "required": ["session_id", "agent_id", "agent_type"],
  "properties": {
    "session_id": {"description": "The runtime's own session identifier.", "type": "string", "minLength": 1},
    "background_tasks": {"description": "What the runtime still had running in the background when this happened, as it listed it: a command, a subagent. Each entry is the runtime's own, with an id, a type, a status and a description, and the command of a shell or the type of a subagent. A task in one list and in no later one has ended; the runtime reports no exit status and no time for it. Absent when the runtime gives no list.", "type": "array", "items": {"type": "object"}},
    "agent_id": {"type": "string", "minLength": 1},
    "agent_type": {"type": "string", "minLength": 1},
    "message": {"description": "The subagent's last message, as text.", "type": "string"}
  }
}
