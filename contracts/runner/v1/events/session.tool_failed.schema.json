{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qory.dev/contracts/runner/v1/events/session.tool_failed.schema.json",
  "title": "dev.qory.session.tool_failed",
  "description": "A tool ran and failed.",
  "type": "object",
  "additionalProperties": false,
  "required": ["session_id", "tool", "tool_use_id", "input", "error"],
  "properties": {
    "session_id": {"description": "The runtime's own session identifier.", "type": "string", "minLength": 1},
    "tool": {"type": "string", "minLength": 1},
    "tool_use_id": {"type": "string", "minLength": 1},
    "input": {"type": "object"},
    "error": {"description": "The failure as the runtime reports it; display text, not a stable format.", "type": "string"},
    "interrupted": {"description": "True when the failure was an abort rather than an error the tool reported.", "type": "boolean"},
    "duration_ms": {"type": "integer", "minimum": 0},
    "agent_id": {"description": "The subagent this happened in, when it did not happen in the main conversation.", "type": "string", "minLength": 1},
    "agent_type": {"description": "The subagent's type, or the agent the session was started as.", "type": "string", "minLength": 1}
  }
}
