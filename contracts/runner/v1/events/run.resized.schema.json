{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qory.dev/contracts/runner/v1/events/run.resized.schema.json",
  "title": "ai.qory.run.resized",
  "description": "The pseudo-terminal the runtime runs on was resized, at the sequence where the new size took effect: the run.log chunks after it were written to a terminal of this size. Only on an interactive run; the size it started with is terminal on ai.qory.run.started.",
  "type": "object",
  "additionalProperties": false,
  "required": ["cols", "rows"],
  "properties": {
    "cols": {"description": "The new width, in columns.", "type": "integer", "minimum": 1, "maximum": 65535},
    "rows": {"description": "The new height, in rows.", "type": "integer", "minimum": 1, "maximum": 65535}
  }
}
