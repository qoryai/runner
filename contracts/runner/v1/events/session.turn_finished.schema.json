{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qory.dev/contracts/runner/v1/events/session.turn_finished.schema.json",
  "title": "dev.qory.session.turn_finished",
  "description": "The runtime finished responding to a prompt.",
  "type": "object",
  "additionalProperties": false,
  "required": ["session_id"],
  "properties": {
    "session_id": {"description": "The runtime's own session identifier.", "type": "string", "minLength": 1},
    "message": {"description": "The last message of the turn, as text.", "type": "string"},
    "background_tasks": {"description": "What the runtime still had running in the background when this happened, as it listed it: a command, a subagent. Each entry is the runtime's own, with an id, a type, a status and a description, and the command of a shell or the type of a subagent. A task in one list and in no list after it has ended; the runtime reports no exit status and no time for it. Absent when the runtime reports no list.", "type": "array", "items": {"type": "object"}},
    "agent_id": {"description": "The subagent this happened in, when it did not happen in the main conversation.", "type": "string", "minLength": 1},
    "agent_type": {"description": "The subagent's type, or the agent the session was started as.", "type": "string", "minLength": 1}
  }
}
