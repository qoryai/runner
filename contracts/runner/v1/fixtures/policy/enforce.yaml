# Deny every connection to a host outside the list, and record the denial. A host deny
# covers is denied though an allow entry covers it.
version: 1
egress:
  mode: enforce
  allow:
    - api.anthropic.com
    - "*.github.com"
    - github.com
    - registry.npmjs.org
  deny:
    - gist.github.com
