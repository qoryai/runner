# Reach a tool of the machine's, which serves a name that exists nowhere, on one prefix
# of its paths. The tool is started for the run with the argument; the proxy hands it
# every request to its host that the path rule lets through.
version: 1
egress:
  mode: enforce
  allow:
    - api.anthropic.com
    - files.tools.internal
  paths:
    files.tools.internal:
      - /media/acme/shop/*
tools:
  - name: files
    argument: acme/shop
