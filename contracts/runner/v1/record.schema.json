{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qory.dev/contracts/runner/v1/record.schema.json",
  "title": "Record",
  "description": "One unit of runtime output as a descriptor reads it: the source it came from and the record itself. The shape of the lines a hook forwarder writes to the local socket, and of a descriptor fixture's records.jsonl.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "source",
    "record"
  ],
  "properties": {
    "source": {
      "description": "output for a JSON line the runtime printed on its standard output; hooks for the JSON a hook command received on its standard input.",
      "type": "string",
      "enum": [
        "output",
        "hooks"
      ]
    },
    "record": {
      "description": "The JSON object as the runtime produced it, unchanged.",
      "type": "object"
    }
  }
}
